
[inputs]
figma_url = "https://www.figma.com/design/mgzCV3zD3iWpctEI6UoUhB/Chakra-UI?node-id=580-1767&p=f&m=dev"
component_repo_path = "../chakra-ui/"

[agent]
# backend = "cli" # options: cli, openai, claude
# cli_command = "codex exec --model gpt-5.1-codex-mini --sandbox read-only"

# Using OpenAI SDK (requires OPENAI_API_KEY environment var)
# backend = "openai"
# sdk_model = "gpt-5.1-codex-mini"

# Using Claude SDK (requires ANTHROPIC_API_KEY environment var)
# [agent]
backend = "claude"
sdk_model = "claude-haiku-4-5"
